/* Pegar acá la URL del Apps Script (la que termina en /exec).
   Se saca de: planilla → Extensiones → Apps Script → Implementar →
   Nueva implementación → Aplicación web → Ejecutar como: Yo ·
   Quién tiene acceso: Cualquier usuario → Implementar → copiar la URL. */
window.GTM_CONFIG = {
  api: ''
};
